# N-1〜N-5 不具合修正の手動検証手順

> 症状1（ブラウザ再起動/初回表示で回答が閉じてしまう）と
> 症状2（別動画遷移で表示がブレる）の回帰防止確認。

## ビルド準備

```bash
npm run build
# dist/ を Chrome で「パッケージ化されていない拡張機能」として読み込み
# chrome://extensions → デベロッパーモード ON → dist/ を選択
```

## 確認する 5 つのシナリオ

### シナリオ 1: ブラウザ再起動時の動作（症状 1-a）
1. 拡張機能を読み込んだ状態で YouTube 動画を開く
2. 動画ページで「📝 A」ボタンを押して要約を生成
3. 完了後、Chrome を **完全に終了** → 再起動
4. 同じ動画ページが BFCache 復元で開かれる
5. **期待動作**: サイドバーが **表示されたまま** になる
   - 旧症状: パネルが閉じてしまう（`#ys-panel` が `display: none`）
6. ✅ 合格条件: パネル DOM が `display: flex` のまま

### シナリオ 2: 初回表示で要約ボタン（症状 1-b）
1. 拡張機能をリロード（chrome://extensions → リロード）
2. YouTube 動画を開く
3. **即座に**「📝 A」ボタンを押す（字幕プリロード前に押す）
4. **期待動作**: パネルが **表示されたまま** 回答が表示される
   - 旧症状: 「⏳ 字幕取得中...」が一瞬表示されてから閉じる
5. ✅ 合格条件: パネルが `display: flex` のまま、回答が正常にストリーミング表示される

### シナリオ 3: 再生成ボタン（症状 1-c）
1. シナリオ 1 または 2 で要約を完了
2. サイドバー内の「🔄 再生成」ボタンを押す
3. **期待動作**: 前の回答が **消えずに** 新回答で上書きされる
   - 旧症状: 一瞬 `#ys-summaryText` が空になり「回答が消えた」ように見える
4. ✅ 合格条件: 旧回答が途中まで残ったまま、新チャンクで上書きされる

### シナリオ 4: 別動画への遷移（症状 2-a）
1. 動画 A を開く → 「📝 A」ボタンで要約生成
2. 別の動画 B へのリンクをクリック（SPA 遷移）
3. 動画 B で「📝 A」ボタンを押す
4. **期待動作**: パネルが点滅せず、**最初から正常表示** される
   - 旧症状: `popstate` + `yt-navigate-finish` + `yt-page-data-updated` の
     3 重 emit とポーリングで `#ys-panel` が閉じては開かれ、表示がブレる
5. ✅ 合格条件: パネルが `display: flex` のまま、回答が正常にストリーミング表示される

### シナリオ 5: 戻る/進むボタン（症状 2-b）
1. 動画 A を開く → 「📝 A」ボタンで要約生成
2. 動画 B へ遷移 → その後、**ブラウザの「戻る」** で動画 A へ戻る
3. **期待動作**: 動画 A の要約結果がそのまま残る
   - 旧症状: `#ys-panel` が閉じて見えなくなる
4. ✅ 合格条件: パネルが `display: flex` のまま、動画 A のキャッシュが復元される

## 確認する 4 つの追加シナリオ

### シナリオ 6: 同一動画ページの再ロード
1. 動画 A を開く → 「📝 A」ボタンで要約生成
2. **ブラウザリロード**（F5）
3. **期待動作**: 起動初期化は走るが、開いた直後で `#ys-panel` が閉じない
4. ✅ 合格条件: ページ読込完了時にパネルが `display: flex`、保存キャッシュが復元される

### シナリオ 7: シーク変化（#t=NN）
1. 動画 A を開く → 「📝 A」ボタンで要約生成
2. プレイヤーの **シークバー** を別の時刻に移動
3. URL が `#t=NN` に変わる
4. **期待動作**: パネルは触られない（シークで閉じない）
5. ✅ 合格条件: `#ys-panel` が `display: flex` のまま

### シナリオ 8: 10 秒後のポーリング
1. 動画 A を開く → 何もしない
2. 10 秒待つ
3. **期待動作**: 同一 URL の再ポーリングで `resetState()` が再発火しない
4. ✅ 合格条件: コンソールログに `[nav/detector] NAV_FINISH dedupe: skip` が出力されない

### シナリオ 9: BFCache 復元
1. 動画 A を開き、要約生成
2. **別タブ** を開き、しばらく作業
3. YouTube タブに戻る
4. **期待動作**: パネルは触られない
5. ✅ 合格条件: パネルが `display: flex` のまま

## 開発者向けデバッグ

DevTools コンソールで `globalThis` を使って状態を確認:

```javascript
// 現在の動画 ID
location.href.match(/[?&]v=([^&]+)/)?.[1];

// 拡張機能の状態（content script から）
chrome.runtime.sendMessage({ action: "ysPing" }, (r) => console.log(r));
```

## ロールバック手順

もし症状が悪化した場合:

```bash
git revert HEAD
npm run build
```

## 自動テストでの回帰確認

```bash
npm test -- --ci
```

追加されたテスト:
- `tests/nav-stability.test.js` — N-1, N-3, N-4
- `tests/ai.test.js` — N-2 (processSingleStream)
- `tests/content-index.test.js` — N-5 (MIN_INIT_INTERVAL_MS)
